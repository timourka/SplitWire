SplitWire 1.2.2 validation
Date: 2026-09-14

Validation environment:
- Linux amd64
- Go 1.23.2
- Python cryptography 46.0.4 (independent WireGuard responder)

1) Diff hygiene
  git diff --check
Result: PASS

2) Unit / integration tests
  go test -count=1 ./...
Result: PASS

Coverage relevant to 1.2.2 includes:
- fan-out file + bounded incremental UI log sink;
- SOCKET EndpointId ownership, wildcard bind attribution and stale-port reuse cases;
- sticky per-flow routing and pre-existing TCP reconnect handling;
- WireGuard DNS parsing / tunneled hostname resolution;
- TCP MSS clamp and Windows LSO software segmentation;
- IPv4/IPv6 fragmentation + bounded reassembly, re-fragmentation and timeout accounting;
- non-blocking WireGuard plaintext queue diagnostics;
- existing NAT, DNS/SNI, policy and WireGuard crypto regressions.

3) Static analysis
  go vet ./...
Result: PASS

4) Race detector
  go test -race -count=1 ./...
Result: PASS

5) Windows-specific static analysis
  GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go vet ./...
Result: PASS

6) Windows x64 GUI cross-build
  GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build -buildvcs=false -trimpath -ldflags '-s -w -H=windowsgui' -o SplitWire.exe ./cmd/splitwire
Result: PASS

Binary identification:
  PE32+ executable for MS Windows 6.01 (GUI), x86-64, 8 sections

SHA-256 SplitWire.exe:
  d79c1cc6c681a99e4d1b1854328502a71927534862be104721fb5fb77ced6ffd

7) Independent WireGuard interoperability smoke test
Python responder: cryptography X25519 + ChaCha20-Poly1305
Go initiator: SplitWire custom WireGuard implementation
Result:
  GO_WIREGUARD_INTEROP_OK
  PY_WIREGUARD_INTEROP_OK

Validation boundary:
The Windows-specific code was cross-built and vetted from Linux. This environment cannot load WinDivert or reproduce the user's live Windows + corporate VPN + Discord/ChatGPT/YouTube environment. Code/build validation is complete, but live end-to-end Windows validation must still be done on a Windows machine.
